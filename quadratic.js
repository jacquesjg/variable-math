const a = 2;
const b = 5;
const c = 3;
const x1 = (-b + (Math.pow(b, 2) - (4 * a * c))) / (2 * a);
const x2 = (-b - (Math.pow(b, 2) - (4 * a * c))) / (2 * a);
console.log("x1=", x1, "and x2=", x2, "when", "a=", a, "b=", b, "and c=", c + ".")